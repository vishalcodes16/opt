# Security Notes

No hidden network calls, no silent remote PowerShell execution, no uncontrolled shell input, no automatic deletion of unknown autoruns, and no security feature removal in the default gaming profile.
