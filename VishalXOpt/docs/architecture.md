# Vishal X Opt Architecture

View → ViewModel → Service Interface → Service Implementation → Windows API / Registry / PowerShell / Device APIs.

Privileged operations are isolated in services and should be gated by preview, backup, confirmation, verification, logging, and rollback.
